[project]
name = "self-healing-automation"
version = "0.1.0"
description = "Policy and GitHub Actions helpers for Claude-assisted E2E repair"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["self_healing*"]
